{
  "framework": "nextjs",
  "buildCommand": "pnpm -w run build:types && pnpm -w run build:schemas && pnpm -w run build:utils && pnpm -w run build:dtos && pnpm run build:client",
  "outputDirectory": "apps/client/.next",
  "installCommand": "pnpm install"
}
